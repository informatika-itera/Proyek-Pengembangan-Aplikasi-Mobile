CREATE TABLE DraftRequisition (
    draftId TEXT NOT NULL PRIMARY KEY,
    projectTitle TEXT NOT NULL,
    currentStep INTEGER NOT NULL,
    lastUpdated INTEGER NOT NULL,
    formStateJson TEXT NOT NULL,
    created_by TEXT NOT NULL
);

insertOrReplaceDraft:
INSERT OR REPLACE INTO DraftRequisition(draftId, projectTitle, currentStep, lastUpdated, formStateJson, created_by)
VALUES (?, ?, ?, ?, ?, ?);

getAllDrafts:
SELECT * FROM DraftRequisition WHERE created_by = ? ORDER BY lastUpdated DESC;

getDraftById:
SELECT * FROM DraftRequisition WHERE draftId = ?;

deleteDraftById:
DELETE FROM DraftRequisition WHERE draftId = ?;
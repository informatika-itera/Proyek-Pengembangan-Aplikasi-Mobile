CREATE TABLE IF NOT EXISTS ActivityHeatmapEntity (
    date TEXT NOT NULL PRIMARY KEY,
    quizCount INTEGER NOT NULL DEFAULT 0,
    notesCount INTEGER NOT NULL DEFAULT 0,
    totalPoints INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE SettingsEntity (
    id INTEGER NOT NULL PRIMARY KEY DEFAULT 1,
    isDarkMode INTEGER NOT NULL DEFAULT 1
);

getSettings:
SELECT * FROM SettingsEntity WHERE id = 1;

upsertSettings:
INSERT INTO SettingsEntity(id, isDarkMode)
VALUES (1, ?)
ON CONFLICT(id) DO UPDATE SET isDarkMode = excluded.isDarkMode;

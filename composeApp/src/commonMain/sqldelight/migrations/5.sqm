CREATE TABLE IF NOT EXISTS PomodoroSessionEntity (
    id TEXT NOT NULL PRIMARY KEY,
    taskId TEXT,
    taskTitle TEXT,
    durationMinutes INTEGER NOT NULL,
    phase TEXT NOT NULL,
    completedAt INTEGER NOT NULL,
    wasCompleted INTEGER NOT NULL DEFAULT 1
);
